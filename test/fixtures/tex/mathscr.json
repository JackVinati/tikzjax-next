{
  "dataset": {
    "texPackages": {
      "mathrsfs": ""
    }
  },
  "note": "upstream #55 #113. KNOWN LIMIT: TeX loads the metric (it is bundled) and the WOFF2 face exists, but dvi2html calls tfmData() itself when parsing the DVI and its built-in table covers only Computer Modern. Fixing this means extending or vendoring @drgrice1/dvi2html.",
  "expect": "unsupported"
}
